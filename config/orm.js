var connection = require("../config/connection.js");

var orm = {

    all: function(table, col, cb){

    },

    create: function(table, cols, vals, cb){

    },

    update: function(table, objColsvals, condition, cb){

    },

    delete: function(table, condition, cb){

    }
}

module.exports = orm;
